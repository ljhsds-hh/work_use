# SpaceMaid 评审记录

本文件是 SpaceMaid 的评审台账。记录格式统一为：**日期 / 范围 / 发现 / 处理 / 结论**。

规则：

- 只记录**真实发生过**的评审与**能在仓库中找到出处**的发现（提交、代码、测试、文档）；
- 无法确认出处的内容一律标注"待确认"，**不编造**；
- 已修复项必须写明修复提交，便于追溯。

> 需求基线的版本与日期以 `Docs/需求.md` 第 19-29 行的「版本记录」表为准；该表随需求文档一并提交，本节引用它作为评审证据。
> 代码阶段提交以 `git log -- SpaceMaid` 为准。本台账编写时的 HEAD 为 `5f6aeae`。

## 一、总览

| 序号 | 日期 | 评审范围 | 发现数 | 处理 | 结论 |
| --- | --- | --- | --- | --- | --- |
| R-1 | 2026-09-20 | 需求文档 v0.6 全文一致性审核 | 16 处（含 4 处冲突/过期引用） | 全部在 v0.6 版本记录中修正 | 通过：需求冻结，无待定项 |
| R-2 | 2026-09-20 | 需求文档 v0.7 第二轮审核（只查内部矛盾与技术准确性） | 7 项 | 全部在 v0.7 版本记录中修正 | 通过：与当前需求一致，可开工 |
| R-3 | 2026-09-20 | 代码阶段：安全底座与清单闭集 | 3 个缺陷 | 见第三节 | 已修复 |
| R-4 | 2026-09-20 | 代码阶段：隔离区与两阶段账本 | 3 个缺陷（含 1 个严重） | 见第三节 | 已修复 |
| R-5 | 2026-09-20 | 代码阶段：静态安全检索测试 | 2 个缺陷 | 见第三节 | 已修复 |
| R-6 | 2026-09-20 | 代码阶段：扫描枚举口径 | 2 个缺陷 | 见第三节 | 已修复 |
| R-7 | 2026-09-20 | 代码阶段：静态检索用例自身能否通过 | 1 个缺陷（自查发现） | 见第三节 | 已修复 |
| R-8 | 2026-09-20 | 独立对抗式代码评审（子代理） | - | **尚未进行** | 待评审 |

## 二、需求文档评审（两轮）

### R-1 需求文档 v0.6 全文一致性审核

| 项目 | 内容 |
| --- | --- |
| 日期 | 2026-09-20 |
| 范围 | `Docs/需求.md` 全文（v0.5 及之前的全部条款） |
| 触发 | 用户终审通过前，要求做一次全文一致性审核 |
| 出处 | `Docs/需求.md` 第 28 行（v0.6 版本记录） |

**发现清单（16 处）**：

| 编号 | 发现 | 处理 |
| --- | --- | --- |
| R1-1 | L1 与回收站是否进隔离区未统一（1.3-5 隔离区定义与 2.1 可还原列不一致） | 统一为"全部分级走隔离区"，修正 1.3-5、2.1 可还原列 |
| R1-2 | 3.3-5 与 5.4-2 冲突：一处允许"记住勾选"，一处要求"不记忆勾选" | 采纳"不记忆勾选"，修正 3.3-5 |
| R1-3 | 2.5 回收站还原表述与统一隔离不一致 | 改为"保留期内可从隔离区还原" |
| R1-4 | 3.1-6 表述会让扫描缓存参与删除决策 | 明确"缓存只用于看，不用于删" |
| R1-5 | 1.1 中存在指向已失效章节的过期引用（指向 9.2） | 修正引用 |
| R1-6 | 1.2-3 漏写回收站 | 补上回收站 |
| R1-7 | 2.3 标题未涵盖新增的 Cookie 项 | 修正标题并补 Cookie 项 |
| R1-8 | 2.4 未把页面文件标为信息项，易被理解为可删 | 标注为信息项（不可勾选、只读展示） |
| R1-9 | 3.2 / 3.4 / 5.1 / 5.2 / 5.3 / 4.3 中"预计释放"口径混乱 | 统一改为"可处理 / 已释放"双口径 |
| R1-10 | 3.7 与 3.8-3 残留已废除的提权步骤 | 删除残留步骤（提权方式已定为固定管理员运行） |
| R1-11 | 5.4 回收站条目缺少"括号写明动作性质"的写法 | 补齐括号字样 |
| R1-12 | 第 7 章验收第 1/2/7 条措辞与"由用户执行""统一隔离"不一致 | 对齐措辞 |
| R1-13 | 第 9 章标题残留"待确认" | 去掉"待确认" |
| R1-14 | 隔离区定义未说明"同卷不释放空间"的代价 | 在 2.1、3.2-6、3.4-7 写明 |
| R1-15 | 清理前未要求预留隔离区空间 | 补充空间预检要求（3.4-2） |
| R1-16 | 图标方案未定 | 定为程序化生成；发布登记按仓库既有约定执行 |

**结论**：**通过**。全部修正随 v0.6 版本记录落库，**需求冻结，无待定项**。

### R-2 需求文档 v0.7 第二轮审核（内部矛盾与技术准确性）

| 项目 | 内容 |
| --- | --- |
| 日期 | 2026-09-20 |
| 范围 | `Docs/需求.md` 全文，**只查内部矛盾与技术准确性**（不做需求扩项） |
| 触发 | 用户要求"再审核一遍" |
| 出处 | `Docs/需求.md` 第 29 行（v0.7 版本记录） |

**发现清单（7 项）**：

| 编号 | 发现 | 性质 | 处理 |
| --- | --- | --- | --- |
| R2-1 | **惰性释放与"无常驻进程"冲突**：原文写隔离区"到期自动释放"，但工具定为"打开就用、用完就关、无常驻进程"，不可能后台定时释放；日志滚动同理 | 真实内部矛盾 | 改为**惰性释放**：在下次启动/扫描时释放已到期批次（3.4-5、3.5-3）。实现落在 `QuarantineService.ReleaseExpired`，由 `CoreServices.Prepare()` 调用 |
| R2-2 | DISM 组件存储清理的技术描述不准确 | 技术准确性 | 明确"只移除被取代的旧组件"，并明确**不得使用重置基线开关**（2.4） |
| R2-3 | 空间预检口径未区分同卷/跨卷，会导致"同卷因空间不足被误拒"或"跨卷清到一半失败" | 技术准确性 | 区分口径：**跨卷才需预留、同卷仅移动目录条目**（3.4-2、3.4-7）。实现落在 `QuarantinePathValidator`（同卷不预检空间） |
| R2-4 | 缩略图缓存被 `explorer.exe` 占用时缺少行为定义 | 技术边界缺失 | 明确：被占用则跳过且**不自动结束用户的资源管理器**，报告中提示"重启资源管理器后可再次清理"（2.2）。实现落在 `l1.thumb-cache` 的后果说明 |
| R2-5 | 应用日志缓存范围过于模糊（易变成"扫全盘 logs"） | 技术边界缺失 | 收紧为**白名单逐条评审**，禁止通配（2.2）。实现落在 `l1.app-logs-vscode` / `l1.app-logs-jetbrains` 逐条登记 |
| R2-6 | 回收站处理范围与实现方式未定 | 技术边界缺失 | 明确**默认仅 C 盘**，并按 SID 子目录**成对处理 `$I`/`$R`**，不整目录递归（2.5）。实现落在 `RecycleBinTargets` |
| R2-7 | 产品范围边界未显式写"只处理 C 盘"；休眠项的副作用影响范围未说清；5.4-1 的 L1 勾选框措辞与实现不符 | 边界/措辞 | 补充"只处理 C 盘"（1.1、第 8 章）；休眠项补充"只影响休眠与正常关机"（2.4、3.8）；修正 5.4-1（L1 无勾选框） |

**结论**：**通过**。审核结论为"与当前需求一致，可开工"。以上 7 项全部在实现中有对应的落地位置，且多数已被单测覆盖（见 `Docs/测试报告.md`）。

## 三、代码阶段缺陷与修复

以下发现均来自真实提交记录。每条都给出提交 hash 与提交信息摘要，便于 `git show` 复核。

### R-3 安全底座与清单闭集

| 编号 | 日期 | 范围 | 发现 | 处理 | 结论 |
| --- | --- | --- | --- | --- | --- |
| RC-1 | 2026-09-20 | `Safety/Denylist.cs`、`Catalog/CatalogValidator.cs` | 禁止清单只有"整体命中"判定，**缺少目录树判定与"用户目录根本身"判定**；而清单自检需要把 | 二者分开：目标根落在 `System32`/`Program Files` 等目录树内，与"目标恰好等于 `%USERPROFILE%`/桌面/文档根"是两类不同违规。缺少这两个方法导致 HEAD **无法独立编译**（工作区里只存在于未提交改动中） | 提交 `5153fdc` fix: 补充禁止清单的目录树与用户目录根判定（IsDeniedTree / IsDeniedUserRoot）。新增 `Denylist.IsDeniedTree` 与 `Denylist.IsDeniedUserRoot` | 已修复 |
| RC-2 | 2026-09-20 | `Catalog/CatalogValidator.cs` 规则 8-3 | 子目录通配模式只校验了 `SubPathPattern` 本身，**没有校验其"固定前缀"是否落在禁止目录树内**——存在"通过通配前缀把目标伸进系统目录"的口子 | 新增规则 8-3：子目录模式的固定前缀（`TargetPathMatcher.GetFixedPrefix`）不得落在禁止目录树内 | 提交 `3f59fc2`（同批新增 `Safety/TargetPathMatcher.cs`） | 已修复 |
| RC-3 | 2026-09-20 | `Models/Enums.cs`、`Catalog/CleanItemCatalog.cs` | 代码注释与产品文案里含有英文开关名 | 主动清理注释里的开关字样，使后续"文本级规则"能保持严格 | 提交 `aece1c9`，并在后续 `5f6aeae` 中完成（见 RC-5） | 已修复 |

### R-4 隔离区与两阶段账本

| 编号 | 日期 | 范围 | 发现 | 处理 | 结论 |
| --- | --- | --- | --- | --- | --- |
| RC-4 | 2026-09-20 | `Quarantine/QuarantineStore.cs` | **批次 Id 同毫秒撞名导致账本被覆盖（严重）**：批次 Id 精确到毫秒（`yyyyMMdd-HHmmss-fff`），同一毫秒内连续清理两次（或**系统时钟被回拨**）会生成同一个批次目录名；后一批直接写进同一目录，**把前一批的 `map.json` 覆盖掉——前一批被搬走的文件从此没有账本记录，等于永久丢失且无法还原** | 改为自动分配唯一批次目录：`AllocateBatch` 循环检测 `DirectoryExists`，撞名则追加 `-1`、`-2` 后缀。代码注释里写明了原因，防止后来者退回"直接拼时间戳" | 提交 `2493193`，提交信息明确记录"修复真实 bug：批次 Id 精确到毫秒，同毫秒连续两批会撞名覆盖账本" | **已修复**（但回归用例存疑，见 R-8 与第五节 L-2） |
| RC-5 | 2026-09-20 | `Quarantine/QuarantineStore.cs` | 搬运顺序若先搬文件再写账本，则断电时会出现"文件搬了但没记账"（文件看起来凭空消失） | 定为**两阶段写盘**：① 先写完整账本且全部条目标 `Pending` -> ② 再逐文件搬运 -> ③ 只保留真正搬进来的条目并改写为已完成。另补 `Recover`：按文件真实位置修复 `Pending` 账目，两边都不存在则标 `Unknown` 交复核报告 | 提交 `bb0dd29`（存储层）、`2493193`（服务层与惰性释放） | 已修复 |
| RC-6 | 2026-09-20 | `Quarantine/QuarantinePathValidator.cs` | 空间预检若不分同卷/跨卷，会出现两种错误：同卷因空间不足被误拒（同卷只是移动目录条目、不占额外空间）；跨卷不预检则"清到一半失败" | 校验按口径分支：仅在**跨卷**时比较目标卷剩余空间，同卷只给 `Warn`（提示"C 盘不会立刻释放"） | 提交 `bb0dd29`，并有"同卷不因空间不足拒绝"的回归用例 | 已修复 |

### R-5 静态安全检索测试

| 编号 | 日期 | 范围 | 发现 | 处理 | 结论 |
| --- | --- | --- | --- | --- | --- |
| RC-7 | 2026-09-20 | `tests/.../Architecture/StaticSafetyTests.cs` | **重置基线断言口径与意图不符**：断言的是**带斜杠**的 `/ResetBase`，而清单文案里写一句"不提供 ResetBase"也能通过——拦不住有人真的把开关拼进命令参数。属于"看着通过、拦不住真问题" | 断言收紧为**裸字串 `ResetBase`**（不区分大小写）在整个 Core 源码中都不出现；同时把 `l3.component-store` 的产品文案改为「本工具不提供「重置基线」选项」，不再出现英文开关名 | 提交 `5f6aeae` fix: 收紧重置基线断言并修正递归通配规则的授权口径。重命名后用例为 `Core_should_not_use_component_store_reset_base`（初版提交为 `aece1c9`） | 已修复 |
| RC-8 | 2026-09-20 | `tests/.../Architecture/StaticSafetyTests.cs` | **静态检索测试自身能否通过**：用例的文本级规则会连**测试文件自己**一起读（`ReadCoreSources()` 扫描整个 Core 源码目录），因此断言里写出的关键字符串会把自己判为违规 | 把 `StaticSafetyTests.cs` 的读取根限定为 `src/SpaceMaid.Core`（`CoreSourceRoot` 从 `AppContext.BaseDirectory` 向上查找），使断言字符串不再自指；并在 `5f6aeae` 中把断言字符串改为裸 `ResetBase` 后仍成立 | `aece1c9` 引入时已用"读取根 = Core 源码"解决；`5f6aeae` 收紧后仍通过 | 已修复 |

### R-6 扫描枚举口径

| 编号 | 日期 | 范围 | 发现 | 处理 | 结论 |
| --- | --- | --- | --- | --- | --- |
| RC-9 | 2026-09-20 | `Catalog/CleanItemCatalog.cs`、`Scanning/` | **缓存型条目形同虚设**：`.nuget\packages`、Chromium 的 `Cache\Cache_Data`、`.gradle\caches` 等缓存的内容都在**子目录**里，而规则只枚举直接子文件，导致这些条目**一个文件都扫不到**（"能清却什么都没清"） | 新增 `WithDefaultDepth`：除**下载目录安装包 / 大文件 / 重复文件 / 卸载残留**（需求 4.1-6 明令不递归用户目录）之外，目录内容规则一律置为递归；扫描与安全闸门本已支持 `Recurse`，无需改内核逻辑 | 提交 `400fb85` fix: 缓存型条目改为递归枚举。新增 `Catalog/CatalogDepthTests.cs` 回归测试（缓存条目必须递归、用户数据条目必须保持非递归，并用真实目录结构做端到端验证） | 已修复 |
| RC-10 | 2026-09-20 | `Safety/TargetPathMatcher.cs` | **递归通配规则的授权口径与扫描口径不一致**：递归的通配规则（如 `*\LocalCache\Temp` + `Recurse`）此前只放行直接子文件，于是出现"扫描收到嵌套文件、安全闸门却全部拒绝"的空转——看着在跑，实际一个都清不掉 | 修正 `TargetPathMatcher`，使递归通配规则放行嵌套文件，同时仍拒绝范围外的路径；新增端到端用例 | 提交 `5f6aeae` | 已修复 |
| RC-11 | 2026-09-20 | `Catalog/CleanItemCatalog.cs` | **静态初始化顺序 bug（自查发现）**：`ShallowOnly` 集合声明在清单构建之后，构建时读到 `null` | 改为无状态 `switch` 判断，去掉对初始化顺序的依赖 | 提交 `400fb85`（提交信息中作为"顺带修掉自己引入的 bug"记录） | 已修复 |

### R-7 提交链完整性

| 编号 | 日期 | 范围 | 发现 | 处理 | 结论 |
| --- | --- | --- | --- | --- | --- |
| RC-12 | 2026-09-20 | 提交链 | 一笔提交的说明称 `Denylist.IsDeniedTree` / `IsDeniedUserRoot` "之前只存在于工作区未提交改动，导致 HEAD 无法独立编译"——即**中间某次提交的 HEAD 是不自洽的**（能编译但是靠工作区脏改动撑着） | 提交 `5153fdc` 补齐这两个方法；后续要求"每次提交后 HEAD 必须能独立编译并通过测试" | 提交 `5153fdc` fix: 补充禁止清单的目录树与用户目录根判定 | 已修复 |

## 四、待评审范围（明确标注）

以下内容**尚未经过独立评审**，如实标注，等代码评审子代理补充：

| 编号 | 待评审内容 | 说明 |
| --- | --- | --- |
| L-1 | **独立对抗式代码评审** | `Docs/实施计划.md` 的检查点 E 要求"Task 15 + 独立评审（子代理对抗式 review）+ 修复"。该轮独立评审**尚未进行**，属"待代码评审子代理补充" |
| L-2 | 批次 Id 撞名的回归用例是否存在 | `2493193` 的提交信息写着"有回归用例"，但当前 `Quarantine/QuarantineStoreTests.cs` 的 8 个用例中**找不到**针对"同毫秒分配两个批次目录"的用例（测试文件里没有涉及 `AllocateBatch` / 批次唯一性的断言）。可能用例在别处，也可能提交信息与代码不符。**待代码评审子代理确认**：若确实缺失，应补一个"同一时刻连续 `StoreCore` 两次，得到两个不同批次目录且两份账本都完好"的用例。**已登记为测试缺口 G-11**（见 `Docs/测试报告.md`） |
| L-3 | 界面层（`SpaceMaid.App`）实现 | **正在收尾**：`App.xaml.cs` 已接入 `CoreServices`，`ViewModels/`（`MainViewModel` 等）与 `Views/`（主窗 + 设置窗）已就位，但**尚未人工核验界面、也未跑通端到端点击流程**。ViewModel 与文案规则的评审**待界面收尾后进行**，届时需补充：不记忆勾选状态的界面验证、L3 二次确认必经、风险红文案非空、同卷提示不含"已释放" |
| L-4 | 实机验收 | 按需求 3.9，真实清理动作由用户在真机执行。**尚未执行**，因此"第 7 章验收第 2、3、7、9 条"的实机部分无法评审 |
| L-5 | 静态检索测试的覆盖范围 | `ReadCoreSources()` 只读 `Code/src/SpaceMaid.Core/**/*.cs`，**界面层不在删除调用白名单的检查范围内**。是否需要扩展覆盖，待评审决定 |

## 五、评审制度约定（后续沿用）

1. **需求评审**：需求文档每次改版都要走一轮全文一致性审核，审核结论写进 `Docs/需求.md` 的版本记录表，并在本台账追加一行；
2. **代码评审**：每个模块交付后由独立子代理做对抗式评审（重点是安全不变量 I-1 至 I-6 是否被破坏），发现写入本台账第三节；
3. **修复必须带证据**：每个修复都要有提交 hash，且提交信息写明"原来会怎样、现在怎么改"（例如"同毫秒撞名会覆盖账本"）；
4. **测试缺口显式登记**：发现"没有测试覆盖"要写进 `Docs/测试报告.md` 的缺口表，不允许静默；
5. **不编造**：无法确认出处的内容标"待确认"，不写"疑似已修复"这类模糊结论；
6. **HEAD 必须自洽**：任何提交后，`dotnet build` 与 `dotnet test` 都必须能独立通过，不允许靠未提交的工作区改动撑着（RC-12 的教训）。
